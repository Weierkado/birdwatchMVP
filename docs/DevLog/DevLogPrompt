你是一名资深 HTML/CSS/JavaScript 游戏项目维护工程师。请根据当前 git diff 和项目文件，把本次开发改动同步到 DevLog 相关文档中。

本次任务只更新开发文档，不做功能开发。

【需要维护的文档】

请同步更新以下文件：

1. docs/DevLog/DEVLOG.md
2. docs/DevLog/DEVLOG_CURRENT_STATUS.md
3. docs/DevLog/CODE_MAP.md

如果当前仓库中路径不同，请优先使用实际存在的同名文件：
- DEVLOG.md
- DEVLOG_CURRENT_STATUS.md
- CODE_MAP.md

【硬性要求】

1. 不修改任何业务代码。
2. 不修改任何数据文件。
3. 不修改任何样式文件。
4. 不修改 index.html。
5. 只允许修改上述 DevLog / Code Map 文档。
6. 不要编造没有发生的改动。
7. 不要把计划写成已完成。
8. 不要把历史状态改成当前状态。
9. 不要删除历史日志，除非只是修正文档内部明显错误。
10. 不要重写整份文档。
11. 只做必要的增量更新。
12. 以当前 git diff / 最近改动为准，而不是凭记忆推断。

====================
一、先阅读当前改动
====================

请先检查当前 git diff，理解本次开发实际改了什么。

重点关注：

- 修改了哪些文件
- 新增了哪些功能
- 修复了哪些 bug
- 调整了哪些 UI / 动画 / 文案
- 是否修改了数据结构
- 是否修改了 LocalStorage key
- 是否修改了状态机流程
- 是否修改了 PHOTO / FOCUS / RESULT / REPOSITION / LOST
- 是否修改了所见即所得
- 是否修改了对焦判定
- 是否修改了图鉴结构
- 是否修改了 snapshot 结构
- 是否修改了电量 / 拍照消耗
- 是否新增或移除了重要边界

如果 git diff 不可用，请根据当前工作区文件与文档内容做保守总结，并明确不要写不确定内容。

====================
二、更新 DEVLOG.md
====================

DEVLOG.md 是按日期记录的开发流水账。

请在当天日期下追加本次改动记录。

日期格式：

## YYYY-MM-DD

如果今天的日期标题已存在：
- 在该日期下追加 bullet。
- 不要新建重复日期标题。

如果今天的日期标题不存在：
- 在文档顶部新增今天日期标题。
- 新增本次 bullet。

写法要求：

- 使用中文。
- 使用 bullet list。
- 每条记录描述一个明确完成项。
- 记录“实际完成了什么”，不要写“计划做什么”。
- 需要写清楚关键边界，例如：
  - 未修改核心流程
  - 未修改存档结构
  - 未修改对焦判定
  - 未接入音效资源
  - 仅 UI / 样式改动
- 如果本次是 bug 修复，要写清楚根因和修复方式。
- 如果本次只是体验优化，也要写清楚“不改变业务语义”。

示例风格：

- 修复 FOCUS shoot 后事件描述重复播放 reveal 动画的问题：根因是 shoot 后主流程 render 与 FOCUS 徽章离场动画结束后的二次 render 都会强制重启 `text-reveal`；新增 `lastEventTextRevealKey` / `getEventTextRevealKey()` 后，同一份事件文本在同一状态下只播放一次 reveal。
- FIRST_ENCOUNTER 新鸟文本改为按中文句号分段，并以逐段淡入上移的方式显示；不使用逐字打字机，不改原文案，不提前泄露正式鸟名，支持 reduced motion 直接显示。
- 本次仅修改 UI 动效和文档，不修改 PHOTO / FOCUS / RESULT / REPOSITION / LOST 流程、对焦判定、所见即所得、LocalStorage 结构或鸟种数据。

不要写：
- “优化了一些东西”
- “修复若干问题”
- “提升用户体验”
除非后面说明具体是什么。

====================
三、更新 DEVLOG_CURRENT_STATUS.md
====================

DEVLOG_CURRENT_STATUS.md 是给未来开发者 / Codex 看的“当前最新状态”。

它的作用不是完整流水账，而是防止后续开发基于旧状态误判。

请只在以下情况更新它：

1. 当前版本的核心状态发生变化。
2. 关键边界发生变化。
3. 某个历史描述已经不再代表当前版本。
4. 新增了未来必须遵守的维护边界。
5. 新增了重要的当前实现事实。

更新方式：

- 优先在顶部“最新状态补充”区域更新。
- 如果已有当天日期区块，就在其中追加或修正。
- 如果没有当天日期区块，就在顶部新增：

## YYYY-MM-DD 最新状态补充

- 不要删除旧日期内容。
- 旧内容如已过时，可以在新顶部说明“旧记录中关于 xxx 的描述作为历史保留，不再代表当前版本”。

写法要求：

- 只写当前仍然有效的状态。
- 不要重复 DEVLOG.md 的所有流水账。
- 重点写“当前是什么样”和“不要误改什么”。

适合写入 CURRENT_STATUS 的内容包括：

- 当前 LocalStorage key 和数据结构。
- 当前 PHOTO 子阶段规则。
- 当前对焦框 / 对焦判定规则。
- 当前图鉴渲染规则。
- 当前 snapshot 字段契约。
- 当前动画生命周期关键边界。
- 当前 UI reveal key 机制。
- 当前禁止恢复的旧逻辑。

示例风格：

- 事件描述 reveal 当前由 `lastEventTextRevealKey` 控制：只有 `mode + photoPhase + eventText/eventHtml` 语义变化时才重播文字出现动画；拍立得退场、FOCUS badge 离场、animation cleanup 造成的二次 render 不应强制重启动画。
- FIRST_ENCOUNTER 文本当前采用按中文句号分段 + 逐段淡入上移，不使用逐字打字机；该效果只作用于首次遇见新鸟文本，普通事件文本仍使用普通 reveal。
- 当前不要恢复主界面对焦框吸附跟随；对焦框视觉位置必须与 `focusEngine` 中心判定保持一致。

如果本次改动不涉及长期状态或关键边界：
- 可以不改 DEVLOG_CURRENT_STATUS.md。
- 但需要在最终输出中说明“CURRENT_STATUS 未更新，因为本次没有长期状态变化”。

====================
四、更新 CODE_MAP.md
====================

CODE_MAP.md 是代码结构地图。

只在以下情况更新：

1. 新增 / 删除 / 重命名了模块。
2. 模块职责发生变化。
3. 重要函数职责发生变化。
4. 状态流转发生变化。
5. 数据结构发生变化。
6. 新增重要 UI 子系统。
7. 新增重要维护风险。
8. 新增后续扩展入口。
9. 某个文档描述已经明显落后于代码。

不要因为普通样式微调就大改 CODE_MAP。

更新位置建议：

- 修改 `入口与渲染层`：当 `main.js` 的渲染职责、动画生命周期、事件分发、reveal key 等机制变化。
- 修改 `业务状态机`：当 `gameSession.js`、mode、photoPhase、action 规则变化。
- 修改 `外部行为序列 vs 焦内序列`：当 `photoSequence.js` 或 `focusSequence.js` 变化。
- 修改 `对焦系统`：当 `focusEngine.js`、`focusConfig.js`、`CAMERA_FOCUS_CONFIG`、对焦判定变化。
- 修改 `所见即所得与卡牌`：当 `cardDraw.js` 或卡牌数据规则变化。
- 修改 `图鉴与加新`：当 `fieldGuide.js`、图鉴 UI、存档、snapshot 变化。
- 修改 `常见维护风险`：当发现新的坑，例如二次 render 重播动画。
- 修改 `QA 快速检查清单`：当新增需要人工回归的关键路径。

写法要求：

- 保持代码地图简洁。
- 以当前真实代码为准。
- 不要写不存在的文件或函数。
- 不要把历史旧流程写成当前流程。
- 不要把设计计划写成代码现状。
- 如果只新增一个维护风险，就只改对应小节。

建议新增维护风险示例：

- 事件文本 reveal 不应在每次 render 都强制重播，应通过 reveal key 判断文本语义是否变化，避免 shoot 后主 render 与动画 cleanup 二次 render 导致文本跳动。
- 拍立得动画 cleanup 不应触发整页 render，除非确实需要刷新业务状态。

====================
五、文档之间的分工
====================

请保持三份文档的分工：

DEVLOG.md：
- 记录“今天做了什么”。
- 可以相对详细。
- 按日期追加。
- 是流水账。

DEVLOG_CURRENT_STATUS.md：
- 记录“当前版本是什么状态”。
- 只写当前有效事实和关键边界。
- 不是完整流水账。
- 用于防止未来误判。

CODE_MAP.md：
- 记录“代码结构和模块职责”。
- 只有结构、职责、流程、风险、扩展入口变化时才更新。
- 不记录每个 UI 小修。

====================
六、输出前自查
====================

完成后请检查：

1. 是否只修改了 DevLog / Code Map 文档。
2. 是否没有修改业务代码。
3. DEVLOG.md 是否有今天日期。
4. DEVLOG.md 是否记录了本次实际改动。
5. DEVLOG_CURRENT_STATUS.md 是否只记录当前有效状态。
6. CODE_MAP.md 是否没有写入不存在的文件或函数。
7. 三份文档是否没有互相复制大量重复内容。
8. 是否没有把计划写成已完成。
9. 是否没有把历史状态覆盖成当前状态。
10. 是否没有删除重要历史记录。
11. Markdown 格式是否正常。
12. 中文表达是否清楚，未来开发者能直接理解。

====================
七、最终输出格式
====================

完成后请只输出：

1. 更新了哪些文档：
   - DEVLOG.md：更新了什么
   - DEVLOG_CURRENT_STATUS.md：更新了什么 / 或说明未更新原因
   - CODE_MAP.md：更新了什么 / 或说明未更新原因

2. 本次文档记录的核心改动摘要。

3. 确认：
   - 未修改业务代码
   - 未修改数据结构
   - 未修改存档 key
   - 未修改核心流程

不要输出整份文档全文。